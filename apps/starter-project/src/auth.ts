// import { initAuth, AuthConfig } from "@pete_keen/north";

// const authConfig: AuthConfig = {
// 	databaseUrl: process.env.DATABASE_URL,
// 	secreyKey: process.env.JWT_SECRET,
// };

// export const auth = initAuth(authConfig);
